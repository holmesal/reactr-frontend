// Generated by CoffeeScript 1.6.2
(function() {
  define(['jquery', 'underscore', 'backbone', 'build/views/controlView'], function($, _, Backbone, ControlView) {
    var PhoneView;

    PhoneView = Backbone.View.extend({
      el: '#phoneScreen',
      initialize: function() {
        this.render();
        return $('#phoneScreen').sortable({
          revert: true,
          receive: this.newItem
        });
      },
      render: function() {
        if (this.collection.length === 0) {
          return console.log('hey');
        }
      },
      getStarted: function() {
        return $('#phoneScreen').animate({
          opacity: 1
        }, 200);
      },
      newItem: function(e, ui) {
        var controlView, dropped;

        dropped = $('#phoneScreen').children().not('.backbone')[0];
        return controlView = new ControlView($(dropped));
      }
    });
    return PhoneView;
  });

}).call(this);
